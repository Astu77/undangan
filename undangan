<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Undangan Pernikahan — [Nama Pengantin]</title>
  <style>
    :root{ --bg:#f8f6f2; --card:#ffffff; --accent:#b77a6b; --muted:#6b6b6b; }
    *{box-sizing:border-box}
    body{font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; margin:0; background:linear-gradient(180deg,var(--bg),#fff); color:#222}
    .container{max-width:980px;margin:36px auto;padding:20px}
    .card{background:var(--card);border-radius:14px;box-shadow:0 8px 30px rgba(10,10,10,0.06);overflow:hidden}

    /* Hero */
    .hero{display:flex;gap:24px;align-items:center;padding:40px}
    .hero .photo{width:180px;height:180px;border-radius:50%;overflow:hidden;flex:0 0 180px;background:linear-gradient(135deg,var(--accent),#c89b8f);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:40px}
    .hero .text h1{margin:0;font-size:28px;letter-spacing:0.6px}
    .hero .text p{margin:8px 0 0;color:var(--muted)}

    /* Details */
    .details{display:grid;grid-template-columns:1fr 1fr;gap:20px;padding:20px}
    .detail-card{padding:18px;border-radius:10px;border:1px solid rgba(10,10,10,0.04)}
    .detail-card h3{margin:0 0 8px}
    .muted{color:var(--muted);font-size:14px}

    /* Countdown */
    .countdown{display:flex;gap:12px;align-items:center;justify-content:center;padding:18px}
    .countdown .unit{background:linear-gradient(180deg,#fff, #fbf7f5);padding:14px 18px;border-radius:10px;min-width:80px;text-align:center}
    .countdown .unit strong{display:block;font-size:20px}
    .countdown .label{font-size:12px;color:var(--muted);margin-top:6px}

    /* RSVP btn */
    .actions{display:flex;gap:12px;padding:20px}
    .btn{padding:10px 16px;border-radius:10px;border:0;cursor:pointer;background:var(--accent);color:#fff;font-weight:600}
    .btn.ghost{background:transparent;color:var(--accent);border:1px solid rgba(183,122,107,0.18)}

    /* Gallery */
    .gallery{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;padding:20px}
    .gallery img{width:100%;height:120px;object-fit:cover;border-radius:8px}

    /* Modal */
    .modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,0.45);display:none;align-items:center;justify-content:center}
    .modal{background:var(--card);width:440px;border-radius:12px;padding:18px;box-shadow:0 20px 40px rgba(10,10,10,0.25)}
    .modal h2{margin:0 0 12px}
    .form-row{display:flex;gap:8px;margin-bottom:8px}
    input[type=text], input[type=email], textarea{width:100%;padding:10px;border-radius:8px;border:1px solid #e6e6e6}

    footer{padding:18px;text-align:center;color:var(--muted);font-size:14px}

    /* Responsive */
    @media (max-width:700px){
      .hero{flex-direction:column;text-align:center}
      .details{grid-template-columns:1fr}
      .gallery{grid-template-columns:repeat(2,1fr)}
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="hero">
        <div class="photo" id="photo">A &amp; B</div>
        <div class="text">
          <h1 id="title">IRFANSYAH &amp; SITI AISYAH</h1>
          <p id="subtitle">Dengan memohon rahmat dan ridho Allah SWT, kami mengundang Bapak/Ibu/Saudara/i untuk menghadiri acara pernikahan kami.</p>
          <p class="muted" id="date">Minggu, 12 Oktober 2025 — 10.00 WIB</p>
        </div>
      </div>

      <div class="details">
        <div class="detail-card">
          <h3>Akad</h3>
          <p class="muted">Lokasi: Masjid Al-Hikmah, Jl. Bahagia No.8</p>
          <p class="muted">Waktu: 09.00 - 10.30</p>
        </div>
        <div class="detail-card">
          <h3>Resepsi</h3>
          <p class="muted">Lokasi: Gedung Serbaguna Nirwana</p>
          <p class="muted">Waktu: 11.00 - 14.00</p>
        </div>

        <div class="detail-card" style="grid-column:span 2">
          <h3>Catatan</h3>
          <p class="muted">Mohon konfirmasi kehadiran (RSVP) paling lambat 7 hari sebelum acara. Busana: Kemeja / Kebaya sopan.</p>
        </div>
      </div>

      <div class="countdown" id="countdown">
        <div class="unit"><strong id="days">0</strong><div class="label">Hari</div></div>
        <div class="unit"><strong id="hours">0</strong><div class="label">Jam</div></div>
        <div class="unit"><strong id="minutes">0</strong><div class="label">Menit</div></div>
        <div class="unit"><strong id="seconds">0</strong><div class="label">Detik</div></div>
      </div>

      <div class="actions">
        <button class="btn" id="rsvpBtn">Konfirmasi Hadir</button>
        <button class="btn ghost" id="directions">Lihat Lokasi</button>
      </div>

      <div class="gallery" id="gallery">
        <img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1?auto=format&fit=crop&w=800&q=60" alt="foto">
        <img src="https://images.unsplash.com/photo-1529626455594-4ff0802cfb7e?auto=format&fit=crop&w=800&q=60" alt="foto">
        <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=800&q=60" alt="foto">
      </div>

      <footer>
        <div>Untuk informasi dan konfirmasi: <strong id="phone">0812-3456-7890</strong></div>
      </footer>
    </div>
  </div>

  <!-- Modal RSVP -->
  <div class="modal-backdrop" id="modal">
    <div class="modal" role="dialog" aria-modal="true" aria-labelledby="rsvpTitle">
      <h2 id="rsvpTitle">Konfirmasi Kehadiran</h2>
      <form id="rsvpForm">
        <div class="form-row"><input type="text" id="nama" placeholder="Nama lengkap" required></div>
        <div class="form-row"><input type="email" id="email" placeholder="Email (opsional)"></div>
        <div class="form-row"><input type="text" id="jumlah" placeholder="Jumlah tamu">
        </div>
        <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:10px">
          <button type="button" class="btn ghost" id="closeModal">Tutup</button>
          <button type="submit" class="btn">Kirim</button>
        </div>
      </form>
    </div>
  </div>

  <script>
    // ----- Konfigurasi mudah -----
    const config = {
      groom: 'Astu Kurniawan',
      bride: '[Nama Pasangan]',
      photoText: 'A & B',
      dateISO: '2025-10-12T10:00:00+07:00', // sesuaikan tanggal & waktu
      phone: '0812-3456-7890'
    }

    // Apply config
    document.getElementById('title').textContent = config.groom + ' \u0026 ' + config.bride
    document.getElementById('photo').textContent = config.photoText
    document.getElementById('phone').textContent = config.phone
    // update readable date
    const dateEl = document.getElementById('date')
    const eventDate = new Date(config.dateISO)
    const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }
    dateEl.textContent = eventDate.toLocaleDateString('id-ID', options) + ' — ' + eventDate.toLocaleTimeString('id-ID', {hour:'2-digit',minute:'2-digit'}) + ' WIB'

    // Countdown
    function updateCountdown(){
      const now = new Date()
      const diff = eventDate - now
      if(diff <= 0){
        document.getElementById('countdown').innerHTML = '<div style="padding:18px;text-align:center;width:100%"><strong>Sudah berlangsung / Selesai</strong></div>'
        clearInterval(timer)
        return
      }
      const days = Math.floor(diff / (1000*60*60*24))
      const hours = Math.floor((diff / (1000*60*60)) % 24)
      const minutes = Math.floor((diff / (1000*60)) % 60)
      const seconds = Math.floor((diff / 1000) % 60)
      document.getElementById('days').textContent = days
      document.getElementById('hours').textContent = hours
      document.getElementById('minutes').textContent = minutes
      document.getElementById('seconds').textContent = seconds
    }
    updateCountdown()
    const timer = setInterval(updateCountdown, 1000)

    // Modal logic
    const modal = document.getElementById('modal')
    document.getElementById('rsvpBtn').addEventListener('click', ()=> modal.style.display = 'flex')
    document.getElementById('closeModal').addEventListener('click', ()=> modal.style.display = 'none')
    modal.addEventListener('click', (e)=>{ if(e.target === modal) modal.style.display = 'none' })

    // RSVP form (demo: local confirmation only)
    document.getElementById('rsvpForm').addEventListener('submit', function(e){
      e.preventDefault()
      const nama = document.getElementById('nama').value.trim()
      const email = document.getElementById('email').value.trim()
      const jumlah = document.getElementById('jumlah').value.trim() || '1'
      if(!nama){ alert('Mohon isi nama lengkap.'); return }
      // Simpan ke localStorage sebagai demo
      const data = {nama, email, jumlah, timestamp: new Date().toISOString()}
      const list = JSON.parse(localStorage.getItem('rsvps') || '[]')
      list.push(data)
      localStorage.setItem('rsvps', JSON.stringify(list))
      alert('Terima kasih, konfirmasi Anda telah diterima.\nKami menunggu kehadiran Anda!')
      modal.style.display = 'none'
      this.reset()
    })

    // Directions action (buka google maps di tab baru)
    document.getElementById('directions').addEventListener('click', ()=>{
      const mapsQuery = encodeURIComponent('Gedung Serbaguna Nirwana, Indonesia')
      window.open('https://www.google.com/maps/search/?api=1&query='+mapsQuery, '_blank')
    })

    // Small enhancement: allow user to download as PNG (simple screenshot)
    // Note: requires browser support and may be limited; provided as convenience.
  </script>
</body>
</html>
